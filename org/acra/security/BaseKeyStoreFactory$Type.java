package org.acra.security;

import I6.a;
import g7.C0996d;

public enum BaseKeyStoreFactory$Type {
    CERTIFICATE,
    KEYSTORE;

    static {
        BaseKeyStoreFactory$Type[] $values;
        $ENTRIES = C0996d.g($values);
    }

    public static a getEntries() {
        return $ENTRIES;
    }
}
